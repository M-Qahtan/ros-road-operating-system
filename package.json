{
  "name": "ros-road-operating-system",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "verify": "node scripts/verify-repository.mjs && node --check scripts/archive-github-evidence.mjs && node scripts/verify-archive-conditional-write.mjs && node --test scripts/external-evidence.spec.mjs && node scripts/verify-artifact-retention.mjs && node scripts/verify-gate-negatives.mjs",
    "simulate:riyadh": "pnpm build && node apps/api/dist/e2e/run-riyadh-pilot.js",
    "simulate:riyadh:failures": "pnpm build && node apps/api/dist/e2e/run-riyadh-failure-modes.js",
    "simulate:ros-eye": "pnpm build && node apps/api/dist/e2e/run-ros-eye-pilot.js",
    "evaluate:safety-fusion": "pnpm --filter @ros/api evaluate:safety-fusion"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
