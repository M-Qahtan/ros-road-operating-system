{
  "name": "@ros/mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node dist/server.js",
    "lint": "pnpm --filter @ros/contracts build && tsc -p tsconfig.json --noEmit",
    "test": "node --test $(find dist -name '*.spec.js' -print | sort)",
    "typecheck": "pnpm --filter @ros/contracts build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ros/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
