# Merge gate summary

Protected checks: `verify`, `postgres-integration`, `staging-smoke`, `riyadh-e2e`, `dependency-review`, and `repository-security`.

Missing, stale, skipped, cancelled, timed-out, or failed checks block merge. CI evidence must match the candidate head, candidate base, and tested merge revisions.
