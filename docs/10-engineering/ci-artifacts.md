# CI artifacts

Required CI artifacts follow this naming pattern:

`job-commit-run-attempt`

The concrete values are the required job name, full commit SHA, workflow run ID, and run attempt. Each artifact contains a manifest with the same identifiers.
