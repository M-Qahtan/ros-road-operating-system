# CI status policy

A required check that is missing, skipped, cancelled, stale, timed out, or failed is unsuccessful and blocks merge. Evidence associated only with another head, base, merge revision, run, or attempt is stale and cannot satisfy the gate.
