# Pull request validation

The pull request for issue #19 must trigger all protected checks and publish fail-closed evidence tied to the exact candidate head, candidate base, and tested merge revisions. Evidence generated for an earlier head or another merge revision is stale.
