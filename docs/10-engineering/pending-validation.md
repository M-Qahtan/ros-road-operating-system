# Pending validation

This change is not complete until the final pull-request head produces successful protected CI and Security runs, every evidence package is validated against its commit identities, and branch protection is proven to reject a deliberately failing candidate.
