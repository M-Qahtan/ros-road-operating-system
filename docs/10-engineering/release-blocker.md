# Release blocker

Until protected checks are enforced, fail-closed evidence is verified, and the ruleset demonstrably rejects a failing candidate, the absence of trustworthy CI evidence remains a release blocker.
