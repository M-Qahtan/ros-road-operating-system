# CI gate purpose

These controls prevent unverified changes from entering `main`, preserve immutable security controls, and provide traceable, fail-closed evidence for every safety-relevant required check.
