# Branch rule target

Target branch: the repository default branch, `main`.

The active ruleset must require pull requests, resolved conversations, an up-to-date branch, and the canonical GitHub Actions checks listed in `required-checks.txt`. Force pushes and deletion are blocked; the bypass list remains empty for routine work.
